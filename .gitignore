# Binaries (root-level build outputs only; anchored so they don't match the
# internal/server and cmd/server* source directories)
/server
/server-rest
/server-dual
/gcp-secret-manager-emulator
bin/
*.exe
*.dll
*.so
*.dylib

# Test binary
*.test

# Output
*.out
coverage.txt
*.prof

# Go workspace file
go.work

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Third-party proto files (downloaded at build time)
third_party/

# SAW agent state (worktree journals, cursors, tool results)
.saw-state/

# SAW worktrees (cleaned up by sawtools cleanup after merge)
.claude/worktrees/
